require('../src/data.js');


describe('Funcion filtrar', () => {
  it('Es una funcion', () => {
    expect(typeof window.handleData).toBe('function');
  });
  
  it('Es una funcion', () => {
    expect(typeof window.handleData).toBe('function');
  });
/*
  it('returns `example`', () => {
    expect(example()).toBe('example');
  });
  */
});
